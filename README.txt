Python bindings to the Eon API
=====================================

This is a client library for Eon built on the Eon API. It
provides a Python API (the ``eonclient`` module) and a command-line tool
(``eon``).
